
# Fetch Company Detail
{
	company(id: "1") {
		id
		name
		description
	}
}

# Fetch User Detail
{
	user(id: "23") {
		id
		firstName
		age
	}
}

# Fragmentation Example
{
  apple: company(id: "2") {
    ...compnayDetails
  },
  
  google: company(id: "2") {
    ...compnayDetails
  }
}

fragment compnayDetails on Company {
  id
  name
  description
}

# Mutation
mutation {
  addUser(firstName: "Stephen", age:26) {
    id
    firstName
    age
  }
}

# Mutation
mutation {
  deleteUser(id: "o8hMcsx") {
    id
  }
}

# Mutation
mutation {
  editUser(id:"1", firstName:"prakhar", age: 21){
    id,
    firstName,
    age
  }
}

